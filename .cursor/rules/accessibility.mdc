---
description: Accessibility - WCAG AA contrast, focus-visible, ARIA patterns, keyboard navigation, reduced motion
globs:
alwaysApply: false
---

# A11y Reference

## WCAG AA Contrast
- Normal text: 4.5:1 | Large (18pt+): 3:1 | UI/focus: 3:1

## Focus
```scss
&:focus-visible { outline: 2px solid var(--color-focus); outline-offset: 2px; }
// ❌ Never: *:focus { outline: none; }
```

## Reduced Motion
```scss
@media (prefers-reduced-motion: reduce) {
	*, *::before, *::after { animation-duration: 0.01ms !important; transition-duration: 0.01ms !important; }
}
```

## Patterns
```php
// Skip link
<a class="skip-link screen-reader-text" href="#main-content"><?php esc_html_e( 'Skip to content', 'skel' ); ?></a>

// Form field
<label for="field-id">Label <span class="required" aria-hidden="true">*</span></label>
<input type="text" id="field-id" required aria-required="true" aria-describedby="field-error" autocomplete="name">
<p id="field-error" class="field-error" hidden aria-live="polite"></p>

// Images: informative
<img src="..." alt="<?php echo esc_attr( $desc ); ?>">
// Images: decorative
<img src="..." alt="" aria-hidden="true">

// Toggle
<button aria-expanded="false" aria-controls="panel-id">Toggle</button>
<div id="panel-id" hidden>Content</div>

// Modal
<div role="dialog" aria-modal="true" aria-labelledby="modal-title" hidden><h2 id="modal-title">Title</h2></div>

// Nav
<nav aria-label="<?php esc_attr_e( 'Primary Navigation', 'skel' ); ?>">
	<button aria-controls="menu" aria-expanded="false"><span class="screen-reader-text">Menu</span></button>
</nav>
```

## Keyboard
```javascript
switch (event.key) {
	case 'Escape': closeModal(); break;
	case 'Tab': /* focus trap */ break;
}
```
